include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - flutter_earth_globe/** # vendored reference package, not part of mapcn_flutter

linter:
  rules:
    public_member_api_docs: false
    prefer_const_constructors: true
